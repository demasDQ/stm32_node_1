# STM32自动控制系统 - 控制模式与通信协议说明

## 功能概述

本系统实现了智能的自动/手动控制模式切换功能，支持通过串口通信远程控制和监控设备状态。

## 控制模式

### 1. 自动模式 (control_mode = 1)
- 系统根据传感器数据自动控制补光和补水
- 补光控制：当光照强度 < 200 lux 时开启，> 800 lux 时关闭
- 补水控制：当土壤电压 < 1.5V 时开启，> 2.5V 时关闭
- 传感器持续监测环境参数并自动调节

### 2. 手动模式 (control_mode = 0)
- 通过串口命令设置开关状态
- 系统不再根据传感器数据自动调节
- 直接按照接收到的手动指令控制设备

## 通信协议

### 帧格式
```
[帧头][命令类型][数据长度][数据域][校验和][帧尾]
  AA      XX        YY      ......    ZZ      55
```

### 字段说明
- **帧头**: 0xAA (固定值)
- **命令类型**: 
  - 0x01: 查询命令
  - 0x02: 设置命令
- **数据长度**: 数据域字节数
- **数据域**: 具体命令数据
- **校验和**: 从帧头到数据域所有字节的累加和
- **帧尾**: 0x55 (固定值)

### 命令详解

#### 1. 查询命令 (CMD_TYPE_QUERY = 0x01)
**请求帧**: `[AA][01][00][校验和][55]`

**响应帧**: 
```
[AA][01][12][节点ID][ADC原始值][ADC电压][温度][湿度][光照值][控制模式][手动补光状态][手动补水状态][实际补光状态][实际补水状态][校验和][55]
```

**响应数据说明**:
- 节点ID: 1字节 (0x01)
- ADC原始值: 2字节 (大端序)
- ADC电压: 4字节 (IEEE 754浮点数)
- 温度: 1字节 (摄氏度)
- 湿度: 1字节 (百分比)
- 光照值: 4字节 (lux，大端序)
- 控制模式: 1字节 (1=自动, 0=手动)
- 手动补光状态: 1字节 (1=开启, 0=关闭) - 手动模式下的设定状态
- 手动补水状态: 1字节 (1=开启, 0=关闭) - 手动模式下的设定状态
- 实际补光状态: 1字节 (1=开启, 0=关闭) - 当前GPIO的实际状态
- 实际补水状态: 1字节 (1=开启, 0=关闭) - 当前GPIO的实际状态

#### 2. 设置命令 (CMD_TYPE_SET = 0x02)
**请求帧**: `[AA][02][02][补光状态][补水状态][校验和][55]`

**数据域说明**:
- 补光状态: 1字节 (非0=开启, 0=关闭)
- 补水状态: 1字节 (非0=开启, 0=关闭)

**响应帧**: `[AA][02][00][校验和][55]` (确认响应)

**功能**:
- 接收到设置命令后，系统自动切换到手动模式
- 根据命令中的状态值设置对应的GPIO输出
- 不再根据传感器数据自动调节

## 使用示例

### 1. 查询当前状态
发送: `AA 01 00 01 55`
接收: `AA 01 12 01 0F A0 40 40 00 00 19 00 00 07 D0 01 00 01 01 00 92 55`
解析:
- 节点ID: 01
- ADC原始值: 0FA0 (4000)
- ADC电压: 40400000 (3.0V)
- 温度: 25°C
- 湿度: 60%
- 光照值: 000007D0 (2000 lux)
- 控制模式: 自动模式
- 手动补光状态: 关闭
- 手动补水状态: 开启
- 实际补光状态: 开启 (当前GPIO状态)
- 实际补水状态: 关闭 (当前GPIO状态)

### 2. 设置手动控制
发送: `AA 02 02 01 00 B5 55`
解析:
- 补光状态: 开启 (01)
- 补水状态: 关闭 (00)
- 校验和: B5

系统将切换到手动模式，开启补光，关闭补水。

## 系统行为

1. **初始化**: 系统默认处于自动模式
2. **模式切换**: 收到设置命令后立即切换到手动模式
3. **状态保持**: 手动模式下保持最后一次设置的状态，直到下次设置命令
4. **传感器监测**: 无论何种模式，传感器数据都会持续采集和更新
5. **状态反馈**: 查询响应包含实际的GPIO状态，便于监控当前执行状态

## 注意事项

1. **校验和计算**: 确保发送的校验和正确，否则帧会被丢弃
2. **帧格式**: 严格按照帧格式发送，包括帧头和帧尾
3. **数据长度**: 查询响应固定18字节数据，设置命令固定2字节数据
4. **模式状态**: 可通过查询命令获取当前控制模式和实际执行状态
5. **状态区别**: 手动状态表示设定值，实际状态表示当前GPIO的真实状态